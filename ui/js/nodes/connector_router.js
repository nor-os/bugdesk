// STUB — see ui/js/__stubs__.js
import { warnStub } from '../__stubs__.js'; warnStub('nodes/connector_router');

export class ConnectorRouter {
    static EVENTS = {
        ADDED:             'connection:added',
        REMOVED:           'connection:removed',
        UPDATED:           'connection:updated',
        GEOMETRY_UPDATED:  'connection:geometry:updated',
    };
    route() { return []; }
}
export default ConnectorRouter;
